# Salidas regenerables de la app (capas DatosConsecuenciaDeFalla)
AnalisisDeDatos/salidas/

# Empaquetado del plugin (scripts/deploy_plugin.py: copia de plugin + core)
dist/

# Cache del tablero (scripts/precompute.py lo regenera)
cache/

# Python
__pycache__/
*.pyc
*.pyo

# GeoPackage journals de SQLite (nunca versionar sueltos)
*.gpkg-wal
*.gpkg-shm
